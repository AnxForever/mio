# Mio 工作交接 — 2026-06-27

> 给下一位接手的人（或下一个会话）。读这一份就能接上。

## 一、今天做了什么（一句话）
从「如何设计 agent 人格」出发，完成三大块 + 一次协作整合：
1. **Per-User 分层人格 L0–L4**（设计 → 计划 → 实现 S0–S6）
2. **AstrBot 技术借鉴**（7 项，全部复刻并通过测试）
3. **部署/运维文档**（微信 VPS 24/7 + QQ 接入 + 掉线邮件告警）
4. 整合并修复了 **codex 并发**做的会话隔离 / WeClaw 微信桥工作

当前分支 **`feat/layered-persona`**，全量 `npm test` 通过，typecheck 干净。

## 二、已完成（带 commit）

### A. 分层人格（L0–L4）
- 设计：`docs/superpowers/specs/2026-06-27-per-user-layered-persona-design.md`（§14 有已知边界 / P1 待办）
- 计划：`docs/superpowers/plans/2026-06-27-per-user-layered-persona.md`
- 实现：`c36d5e3`(S0 类型/路径/读写) → `a028eb6`(S1 L0 Kernel) → `fd467af`(S2 L1→L2) → `fd87cbc`(S3 L3 偏好) → S4(捏人捕获) → `9b9252f`(S5 L4 共同史) → `659b9db`(S6 接入测试)
- 关键文件：`src/persona/layered.ts`、`src/memory/persona-delta.ts`、`src/persona/directive-capture.ts`
- 语义：L0 Kernel（critical 不可裁，守"真实的人"底线）/ L1 archetype（soul.md）/ L2 persona-delta（per-user 可改人设）/ L3 preference / L4 共同史（昵称/回忆）

### B. AstrBot 借鉴（7 项；详见 `docs/astrbot-vs-mio-borrow-plan.md` §8）
- RRF 混合检索 `95c0e4d`（`vector.ts`）
- 知识库 RAG `e87abbb`/`4044d8a`/`ba79557`（`src/memory/knowledge-base/` + `recall_knowledge` 工具 + `scripts/kb-ingest.ts`）
- 压缩升级 `b05e38a`（回合感知 token 预算 + 二分兜底）
- modality 门控 `909307c`（接通 `supportsVision`）
- 工具循环护栏 `a4124c3`（max-step 强制总结 + streak 警告）
- per-persona 工具白名单 `bbe1c6c`（`PersonaDelta.allowedTools` + `scopedToolRegistry`）
- begin_dialogs `2f56e8c`（`PersonaDelta.beginDialogs`）
- 明确**不复刻**：插件市场化/热重载、全量 9 段 pipeline、DB 化人格库（单用户陪伴抽象税）

### C. 会话隔离 / WeClaw（codex 实现 + 本会话整合修复 `a5148fe`）
- 修 `directive-capture` 隔离漏洞：isolated session 的 nickname/共同记忆只写 per-user，不碰全局
- 修 proactive 兼容回归：非 WeClaw 部署保留全局 Telegram/webhook，全局不回退 `MIO_WECLAW_TO`

### D. 部署 / 运维
- `docs/wechat-bridge-vps-deploy.md` — VPS 24/7（systemd 自启 + 备份 + §8 掉线邮件告警）
- `docs/qq-bridge-deploy.md` — QQ 接入（NapCat/OneBot；调研结论：个人伴侣 QQ 比微信更友好）
- `scripts/wechat-bridge/health-alert.sh` — 掉线 → 邮件告警（cron，状态切换才发）

## 三、⚠️ 关键上下文（接手必读）
- **两个 codex 实例并发**在同一 working tree / 同一分支做 per-user / WeChat(WeClaw) / IM 会话隔离。改共享文件（`types.ts`/`persona-delta.ts`/`agent-loop.ts`/`tool-runtime.ts`/`package.json`）前**先 `git status` 核实**；提交时**只 `git add` 自己的文件**；**绝不 amend/rebase 别人的 commit**。types/package.json 是高频热点，必要时用 node 脚本原子改避开 Edit race。
- 约定：ESM 导入带 `.js`；production 代码禁 `console.log`（用 `logger`）；测试 import 自 `dist/`（先 `npm run build`）、`MIO_PROVIDER=mock` 跑；所有路径经 `src/memory/paths.ts`；commit 身份 AnxForever。

## 四、待办 / 下一步
1. **部署**（用户操作）：VPS 跑桥 + 微信/QQ 用**小号**扫码登录。
2. **聊一段后做体检**：把目标机器的 `data/transcripts` + `data/memory-bank` 拉回，分析纸板感 / 记忆有没有真攒住（durableFacts/共同史）/ 多联系人是否串户 / RRF 检索与压缩是否丢上下文 → 找优化点。
3. **P1 多用户完整**（codex 主导）：L4 全局 → per-user 一致、跨重启持久化、runTurn 层持久化测试（见分层人格 design §14）。
4. 邮件告警的 `ALERT_LOG_PATTERN` 要按 `weclaw.log` 实际掉线输出实测调整。

## 五、怎么验证
```bash
npm run build && npm test          # 全量，应全绿
npm run typecheck                  # 干净
```
分层人格专项：`tests/unit-layered-persona.ts`、`tests/unit-rrf-fusion.ts`、`tests/unit-knowledge-base.ts`、`tests/unit-compression.ts`、`tests/unit-modality.ts`、`tests/unit-inference-guardrails.ts`、`tests/unit-directive-isolation.ts`、`tests/unit-persona-tool-allowlist.ts`、`tests/unit-begin-dialogs.ts`
